<Page
    x:Class="WeatherApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WeatherApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="cityTemplate">
            <StackPanel Margin="2,4,2,4" Tapped="StackPanel_Tapped">
                <TextBlock Text="{Binding Path=name}" 
                                       Style="{StaticResource TitleTextBlockStyle}" />
                <TextBlock Text="{Binding Path=countrycode}" 
                                       Style="{StaticResource SubtitleTextBlockStyle}" />
            </StackPanel>
        </DataTemplate>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel  x:Name="spCities" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Orientation="Horizontal" Height="200" VerticalAlignment="Top"/>
        <Rectangle Opacity="0.5" Fill="Black" Visibility="{Binding Visibility,ElementName=fpFlyoutDetails}"/>
        <FlyoutPresenter  Width="330"
                         Height="500" x:Name="fpFlyoutDetails"
                         Visibility="Collapsed" Margin="10"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center">
            <StackPanel HorizontalAlignment="Left" Width="300">
                
                    <TextBlock Text="Select city" HorizontalAlignment="Center" 
                   Style="{StaticResource TitleTextBlockStyle}"
                   x:Name="tblTitle" />
                    <ListView x:Name="lvCities" 
                      BorderBrush="Blue" BorderThickness="3"
                      Height="300"
                      ItemTemplate="{StaticResource cityTemplate}">
                    </ListView>
                <Button x:Name="btnFlyoutClose" Content="Close" Tapped="btnFlyoutClose_Tapped"/>
                </StackPanel>
            
        </FlyoutPresenter>
    </Grid>
</Page>
